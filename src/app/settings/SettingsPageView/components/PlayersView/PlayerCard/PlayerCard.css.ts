import { style } from "@vanilla-extract/css";

/** Как в турнирном списке: нет подписанного договора. */
export const playerCardNoAgreementCls = style({
  backgroundColor: "rgba(255, 196, 2, 0.22)",
});
