<!-- archi:start -->
## Architecture tasks
For any architecture-related work (modeling systems, components, relationships, requirements, specs), use the `archi` CLI and its skills (`archi`, `arch-existing`, `archi-plan`, `archi-implement`, `code-link`, `archi-stress`, `archi-report`, `archi-explain`). Do not read or hand-edit `.fractal/` files directly — query the spec through `archi` subcommands instead. Run `archi help` or `archi guide` for details.

No silent assumptions. State what you're assuming. Surface tradeoffs.Minimal design that solves the problem. No speculative features.
<!-- archi:end -->
